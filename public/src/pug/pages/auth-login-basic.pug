//- * * * * * * * *
//- * * Layout  * *
//- * * * * * * * *

//- The page layout is set using the Pug `extends` directive with a path to a layout template
//- All of the layout options are contained in src/pug/layouts
extends ../layouts/authentication.pug

//- * * * * * * * *
//- * * Config  * *
//- * * * * * * * *

//- These are page-level configuration options that should be set for each page
block config
    - var pageTitle = 'Login'

//- * * * * * * * * * * *
//- * * Page Content  * *
//- * * * * * * * * * * *

//- Define the main `block content` from the layout
block content
    .container-xl.px-4
        .row.justify-content-center
            .col-lg-5
                // Basic login form
                .card.shadow-lg.border-0.rounded-lg.mt-5
                    .card-header.justify-content-center
                        h3.fw-light.my-4 Login
                    .card-body
                        // Login form
                        form
                            // Form Group (email address)
                            .mb-3
                                label.small.mb-1(for='inputEmailAddress') Email
                                input#inputEmailAddress.form-control(type='email', placeholder='Enter email address')
                            // Form Group (password)
                            .mb-3
                                label.small.mb-1(for='inputPassword') Password
                                input#inputPassword.form-control(type='password', placeholder='Enter password')
                            // Form Group (remember password checkbox)
                            .mb-3
                                .form-check
                                    input#rememberPasswordCheck.form-check-input(type='checkbox', value='')
                                    label.form-check-label(for='rememberPasswordCheck')
                                        | Remember password
                            // Form Group (login box)
                            .d-flex.align-items-center.justify-content-between.mt-4.mb-0
                                a.small(href='auth-password-basic.html') Forgot Password?
                                a.btn.btn-primary(href='dashboard-1.html') Login
                    .card-footer.text-center
                        .small
                            a(href='auth-register-basic.html') Need an account? Sign up!

//- * * * * * * * *
//- * * Footer  * *
//- * * * * * * * *

//- Define the `block footer` from the layout
block footer

    //- Bring in the footer mixin and configure
    //- For all available config options for this mixin, visit https://docs.startbootstrap.co/sb-admin-pro/
    include mixins/footer.pug
    +footer({
        footerStyle: 'footer-dark'
    })